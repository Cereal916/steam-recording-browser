<Window x:Class="SteamRecordingBrowser.ExportProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Exporting MP4"
        Width="520"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#0D1015"
        Foreground="#F4F7FB"
        FontFamily="Segoe UI">
    <Window.Resources>
        <Style TargetType="ProgressBar">
            <Setter Property="Background" Value="#11151C"/>
            <Setter Property="Foreground" Value="#66C0F4"/>
            <Setter Property="BorderBrush" Value="#343C49"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Border x:Name="PART_Track"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                ClipToBounds="True">
                            <Border x:Name="PART_Indicator"
                                    Background="{TemplateBinding Foreground}"
                                    HorizontalAlignment="Left"
                                    CornerRadius="3"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="26">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="Exporting MP4" FontSize="20" FontWeight="SemiBold"/>
            <TextBlock x:Name="StatusText" Text="Preparing export…"
                       Foreground="#D5DBE5" Margin="0,10,0,0"
                       TextWrapping="Wrap" LineHeight="19"/>
        </StackPanel>
        <ProgressBar x:Name="ExportProgress" Grid.Row="1"
                     Height="8" Minimum="0" Maximum="100"
                     Margin="0,20,0,0"/>
        <Button x:Name="CancelButton" Grid.Row="2"
                Content="Cancel" Width="96" Margin="0,20,0,0"
                HorizontalAlignment="Right" Click="Cancel_Click"/>
    </Grid>
</Window>
